# Aurevia Jewelry Store

This repository contains the Aurevia jewelry storefront built with React, Vite, Tailwind CSS, and React Router.

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
